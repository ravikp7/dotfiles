function fish_user_key_bindings
  bind -m visual h backward-char force-repaint
  bind -m visual l forward-char force-repaint
  bind -m visual k up-line force-repaint
  bind -m visual j down-line force-repaint
end
